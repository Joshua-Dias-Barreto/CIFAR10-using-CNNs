# <div align="center">CIFAR10-using-CNNs</div>
A Convolutional Neural Network ( CNN ) is constructed and trained to identify whether an image is an airplane, car, bird, cat, deer, dog, frog, horse, ship or a truck.
